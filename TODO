- export +=
- export bug : `export -a kek`, should return instead of registering kek in the env
- declare extern int g_exit_status
- when a function errors edit g_exit_status
- sort export keys
- handle export options, lackof more of
- make the other builtins
- make a diff in printing between env and export
